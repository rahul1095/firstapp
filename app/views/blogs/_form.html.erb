<%= form_with(model: blog, local: true) do |form| %>
  <% if blog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(blog.errors.count, "error") %> prohibited this blog from being saved:</h2>

      <ul>
      <% blog.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :decription %>
    <%= form.text_area :decription, id: :blog_decription %>
  </div>

  <div class="field">
    <%= form.label :is_active %>
    <%= form.check_box :is_active, id: :blog_is_active %>
  </div>

  <div class="field">
    <%= form.label :status %>
    <%= form.text_field :status, id: :blog_status %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= form.text_field :image, id: :blog_image %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
